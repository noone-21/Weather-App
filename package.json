{
  "name": "weatherapp",
  "version": "1.0.0",
  "description": "creating a nodejs weather app",
  "main": "env-cmd -f ./config/dev.env index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "env-cmd -f ./config/dev.env nodemon index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "env-cmd": "^10.1.0",
    "nodemon": "^2.0.19",
    "requests": "^0.3.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.19"
  }
}
